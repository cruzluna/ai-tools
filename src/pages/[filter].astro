---
import IndexPage from "@/pages/index.astro"

export function getStaticPaths() {
  return ["environment", "sandboxes", "cli-tui", "agents-sdk", "autonomous-agents"].map((filter) => ({
    params: { filter },
  }))
}
---

<IndexPage />
